'use client'

import { styled } from '@mui/system'

export const TracksCardStyled = styled('div')(() => ({
    '.tracks-content': {
        display: 'flex',
        flexDirection: 'column',
        gap: '16px',
        color: '#FFFFFF',
        padding: '50px'
    },
}))
