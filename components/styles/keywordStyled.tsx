'use client'

import { styled } from '@mui/system'

export const KeywordStyled = styled('div')(() => ({
    textAlign: 'center',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    flexDirection: 'row',
    gap: '40px',
    marginLeft: '40px'
}))
