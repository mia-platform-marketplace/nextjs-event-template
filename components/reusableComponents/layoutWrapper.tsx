"use client";

function LayoutWrapper({ children }: { children: React.ReactNode }) {
    return (
        <>
            {/* Add you scripts */}
            {children}
        </>
    );
}

export default LayoutWrapper;
